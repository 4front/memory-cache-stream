{
  "name": "memory-cache-stream",
  "version": "1.2.0",
  "description": "In-memory cache modeled after node_redis api with additional streaming support.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks test/"
  },
  "keywords": [
    "memory",
    "cache",
    "streams",
    "redis"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dvonlehman/memory-cache-stream"
  },
  "author": "David Von Lehman (https://github.com/dvonlehman)",
  "license": "MIT",
  "dependencies": {
    "lodash.isfunction": "^3.0.8",
    "lodash.isundefined": "^3.0.1",
    "lodash.keys": "^4.0.6",
    "through2": "^0.6.3"
  },
  "devDependencies": {
    "express": "^4.13.1",
    "istanbul": "^0.3.6",
    "lorem-ipsum": "^1.0.1",
    "mocha": "^2.1.0",
    "simple-bufferstream": "^1.0.0",
    "sinon": "^1.12.2",
    "supertest": "^1.0.1"
  }
}
